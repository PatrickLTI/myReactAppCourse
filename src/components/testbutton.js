// const ThemeButton = () =>
// {
//     const {isBasic} = useContext(ThemeContext);
//     const handleClick = (isBasic) => {
//         return isBasic ? 'Basic Theme' : 'Fancy Theme';
//     }
//     if (isBasic === true)
//         return <button value="submit" onClick={handleClick.bind(this, isBasic)}/>
//     else if (!isBasic)
//         return <Button onClick={handleClick.bind(this, isBasic)}/>
// }
// export default ThemeButton;